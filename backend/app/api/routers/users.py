from fastapi import APIRouter

router = APIRouter()


# Endpoints will be implemented bot-and-auth git-branch
# POST /api/auth  — register/login via Telegram initData
# GET  /api/me    — get current user profile + wallet